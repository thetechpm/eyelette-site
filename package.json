{
  "name": "eyelette",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npx http-server . -p 5173 -c-1 -o",
    "build": "echo 'No build needed - static files only'",
    "preview": "npx http-server . -p 5173"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
